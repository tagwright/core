# Build output
/dist/
*.exe
*.test
*.out
*.prof

# Local dev
.env
*.local

# Never commit decrypted secrets or a live secrets dir
*.sops.decrypted
/run/
/secrets/

# Central docs system: docs/, CLAUDE.md, and CHARTER.md live on disk here but
# are versioned centrally into the projects-docs repo, never in this public
# repo. See the projects and tagwright CLAUDE.md.
/docs/
/CLAUDE.md
/CHARTER.md
